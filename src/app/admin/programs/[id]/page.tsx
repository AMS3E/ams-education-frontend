import ProgramDetailsForm from "@/components/admin/programs/ProgramDetailsForm";

// Details tab — "what the program is". The [id] layout supplies the header,
// tab bar and column; this renders just the Details card.
export default function ProgramDetailsTab() {
  return <ProgramDetailsForm />;
}
